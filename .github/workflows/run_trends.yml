name: Test R Workflow

on:
  workflow_dispatch:

jobs:
  test-r:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install R
        uses: r-lib/actions/setup-r@v2
      - name: Test R
        run: Rscript --version
